async def get_available_blocks(self) -> dict:
    # TODO: get available blocks from /data folder
    return


async def merge_blocks_to_file(self):
    # TODO: Should merge selected blocks, get them from /data folder and save in one file, e.g. as
    # the name we could use combination on blocks' ids. It allows us to cache previous resolut and not
    # repeat the whole process again.
    return


async def build_workflow(self, blocks: dict) -> str:
    # TODO: Should return link to created file in tmp folder.
    return
